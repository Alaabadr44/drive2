export const config = {
  // Toggle this to show/hide debug tools like the logout button and socket event tracker
  enableDebugTools: false,
  // Toggle to show the debug credentials dropdown on the login screen
  enableLoginDebug: false,
};
